		{% if (post.thumb) %}
		<a href="{{site.baseurl}}{{post.url}}">
		{% assign x = post.thumb|slice:0,4 %}
		{% if x == "http" %}
			<img src="{{post.thumb}}" alt="{{post.title}}" width="150px" class="pull-left img-thumbnail"/>
		{% else %}
			<img src="{{site.asseturl}}/{{post.thumb}}" alt="{{post.title}}" width="150px" class="pull-left img-thumbnail"/>
		{% endif %}
		</a>
		{% endif %}