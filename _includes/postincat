{% if (page.categories) %}
<ul class="nav nav-pills nav-stacked">
{% for cat in page.categories %}
	<li class="active"><a href="{{site.baseurl}}/{{cat}}">{{site.lang.samecat}}</a></li>
	{% for post in site.categories[cat] limit:12 %}
	<li><a href="{{site.baseurl}}{{post.url}}">{{post.title}}</a></li>
	{% endfor %}
{% endfor %}
</ul>
{% endif %}