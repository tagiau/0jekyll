{% if (post.tags) %}
{% for tag in post.tags %}
<i class="glyphicon glyphicon-folder-open">:</i>
<span><a class="btn btn-link" href="{{site.baseurl}}/tag#{{tag}}">{{tag}}</a></span>
{% endfor %}
{% endif %}